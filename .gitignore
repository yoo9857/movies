# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# env — 비밀값은 절대 커밋하지 않는다
.env
.env.local
.env.*.local

# database (개발용 SQLite)
*.db
*.db-journal
*.db-wal
*.db-shm

# prisma generated client
packages/db/src/generated/

# misc
.DS_Store
*.pem
*.log
.vercel
.turbo

# locally-stored uploads (object storage takes over when configured)
var/uploads/

# persona portrait sources (registered to server storage; not repo assets)
/Amara.png
/Dorothy.png
/Marcus.png
/vera.png
# same kind of thing: a root-level image dropped in locally, referenced by
# nothing in the tree. Assets the app actually serves live in apps/web/public/.
/gb.png
