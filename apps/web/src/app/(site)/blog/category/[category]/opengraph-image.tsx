// The site card again: a shelf is a listing, and its five headings are not
// worth five hand-drawn cards. See lib/og-site-image.tsx for why this is a
// re-export rather than inheritance.
export { default, alt, size, contentType } from "@/lib/og-site-image";

export const dynamic = "force-dynamic";
